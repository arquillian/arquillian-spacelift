language: java
sudo: required
addons:
  apt:
    packages:
      - openjdk-6-jdk
jdk:
    - openjdk8
    - openjdk7
    - openjdk6
    - oraclejdk9
    - oraclejdk11
branches:
    except:    
        - /^([0-9]+)\.([0-9]+)\.([0-9]+)(?:-([0-9A-Za-z-]+(?:\.[0-9A-Za-z-]+)*))?(?:\+[0-9A-Za-z-]+)?$/
install:
    - true
script:
    - mvn verify
